# Kinematics solver for the FR3 arm (real robot).
# Uses pick_ik (same as simulation) — maintains gradient-descent seeding
# to prevent joint-space jumps during Cartesian interpolation.

fr3_arm:
  kinematics_solver: pick_ik/PickIkPlugin
  kinematics_solver_timeout: 0.05
  kinematics_solver_attempts: 3
  mode: local
  position_threshold: 0.001
  orientation_threshold: 0.01
  cost_threshold: 0.001
  stop_optimization_on_valid_solution: true
  approximate_solution_position_threshold: 0.005
  approximate_solution_orientation_threshold: 0.05

# fr3_hand (gripper) is a parallel actuator, not a kinematic chain — no IK solver.
